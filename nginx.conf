events {}

http {
  server {
    listen 80;

    location / {
      root /usr/share/nginx/html;
      index index.html index.htm;
    }

    location /launch-sso.html {
      root /usr/share/nginx/html;
    }
  }
}

